modules = ["python-3.11"]

[nix]
channel = "stable-24_05"
packages = ["libxcrypt", "postgresql"]

[workflows]
runButton = "Project"

[[workflows.workflow]]
name = "Project"
mode = "parallel"
author = "agent"

[[workflows.workflow.tasks]]
task = "workflow.run"
args = "HUBB Assist Backend"

[[workflows.workflow]]
name = "HUBB Assist Backend"
author = "agent"

[[workflows.workflow.tasks]]
task = "shell.exec"
args = "cd backend && uvicorn app.main:app --host=0.0.0.0 --port=5000 --reload"
waitForPort = 5000

[deployment]
run = ["sh", "-c", "cd backend && uvicorn app.main:app --host=0.0.0.0 --port=5000 --reload"]
